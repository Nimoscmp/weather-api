const Header = ({title}) => {
    return (  
        <>
            <header>
                <nav>
                    <h1>{title}</h1>
                </nav>
            </header>
        </>
    );
}
 
export default Header;